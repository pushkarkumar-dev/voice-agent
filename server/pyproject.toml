[project]
name = "voiceagent-server"
version = "0.1.0"
description = "Model services for the Windows GPU box (STT now, TTS next)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "python-multipart>=0.0.9",
    "faster-whisper>=1.1",
]

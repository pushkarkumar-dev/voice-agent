[project]
name = "voiceagent"
version = "0.1.0"
description = "Local bilingual voice agent: STT -> LLM -> TTS, all on home hardware"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "numpy>=2.0",
    "sounddevice>=0.5",
]
